#
# Exercise 2: Reconnaissance and Output Parsing Guided Version
#
# Instructions:
#     Each command skeleton is already in place.
#     Replace every "???" placeholder with the correct value.
#     Run with: attackmate exercise_02_recon_and_parse_guided.yml
#     Try --debug to see regex match details.
#
# nmap output for an open port looks like:
#     "21/tcp open  ftp"
#     "22/tcp open  ssh"
#     "80/tcp open  http"
#

vars:
  # TODO 1: Replace "???" with your Metasploitable2 IP address
  TARGET: "???"

commands:
  # === Part A: Port Scanning ===

  # TODO 2: Replace "???" with an nmap command that scans ports 21, 22, and 80
  - type: shell
    cmd: "???"

  # TODO 3: Replace each "???" below:
  #   - The regex pattern should match an HTTP service line and capture the port number
  #     (capture group = the part you want to extract)
  #   - The output variable HTTP_PORT should receive the first capture group ($MATCH_0)
  - type: regex
    cmd: "???"
    output:
      HTTP_PORT: "???"

  # TODO 4: Replace "???" with a string that prints the HTTP port
  # Expected output: "HTTP port: 80"
  - type: debug
    cmd: "???"

  # === Part B: Conditional Follow-Up ===

  # TODO 5: Replace "???" in only_if with the condition that checks HTTP_PORT equals 80
  # only the condition needs filling in
  - type: shell
    cmd: "curl -s -o /dev/null -w '%{http_code}' http://$TARGET"
    only_if: "???"

  # TODO 6: Replace "???" with the string to print the HTTP status code
  # Expected output: "HTTP status code: 200"
  - type: debug
    cmd: "???"

  # === Part C: Service Version Extraction ===

  # TODO 7: Replace "???" with an nmap version-detection scan of port 21 on $TARGET
  - type: shell
    cmd: "???"

  # TODO 8a: Extract the full FTP line from nmap output into FTP_LINE
  # Replace "???" with a regex that captures the entire "21/tcp ..." line
  - type: regex
    cmd: "???"
    output:
      FTP_LINE: "$MATCH_0"

  # TODO 8b: Split FTP_LINE on whitespace to isolate individual tokens.
  # Replace each "???":
  #   - input: the variable name to split (not $VAR, just the name)
  #   - cmd: the whitespace pattern to split on (hint: "\ +")
  #   - output variable FTP_SERVICE should hold the service name token (MATCH_3)
  # Token positions after splitting "21/tcp open  ftp  vsftpd 2.3.4":
  #   MATCH_0=21/tcp  MATCH_1=open  MATCH_2=ftp  MATCH_3=vsftpd  MATCH_4=2.3.4
  - type: regex
    input: "???"
    cmd: "???"
    mode: split
    output:
      FTP_SERVICE: "???"

  # TODO 9: Replace "???" with the string to print the FTP service name
  # Expected output: "FTP service: vsftpd"
  - type: debug
    cmd: "???"
